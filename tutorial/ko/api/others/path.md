# PATH



`PATH` 속성은 일반적으로 사용자 정의 컴포넌트 또는 페이지 컴포넌트에 사용되며, 해당 컴포넌트의 등록된 컴포넌트의 파일 주소를 가져오는 데 사용됩니다. 이는 개발 과정에서 컴포넌트의 출처를 이해하는 데 도움이 되며, 특히 다른 리소스 파일을 참조하거나 로드해야 할 때 `PATH` 속성을 사용하여 파일 경로를 구성할 수 있습니다.

아래는 간단한 예시입니다. 사용자 정의 컴포넌트에서 `PATH` 속성을 사용하는 방법을 보여줍니다:

```javascript
const componentPath = $("#myCustomComponent").PATH;
```

이 예시에서는 `id`가 "myCustomComponent"인 `my-comp` 요소를 선택한 후, `PATH` 속성을 통해 해당 사용자 정의 컴포넌트의 파일 경로를 가져왔습니다. 필요에 따라 스크립트 부분에서 `componentPath` 변수를 사용할 수 있습니다. 예를 들어, 이를 이용해 다른 리소스 파일의 경로를 구성하거나 다른 작업을 수행할 수 있습니다.